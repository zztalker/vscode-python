# Overview:
# Generic jobs template to compile and build extension

jobs:
- job: Compile
  variables:
    build: false
  pool:
    vmImage: "macos-latest"
  steps:
    - template: ../steps/build_compile.yml

- job: Build
  variables:
    build: true
  pool:
    vmImage: "macos-latest"
  steps:
    - template: ../steps/build_compile.yml
